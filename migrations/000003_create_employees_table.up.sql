CREATE TABLE employee (
                                id SERIAL PRIMARY KEY,
                                name varchar(250) NOT NULL,
                                age INT NOT NULL,
                                job VARCHAR(250) NOT NULL

);
